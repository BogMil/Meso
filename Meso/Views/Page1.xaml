<Page x:Class="Meso.Views.Page1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Meso.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid >
        <TextBlock Text="asdaaaaaaaaaa" Foreground="Black"/>
        <StackPanel>
            <TextBox Text="{Binding text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="500" Background="white"/>
            <TextBox Text="{Binding text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="500" HorizontalAlignment="Left" Background="white"/>
            <TextBox Text="{Binding text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="500" HorizontalAlignment="Right" Background="white"/>
            <TextBlock Text="{Binding text}"  Foreground="black" Background="AliceBlue"  HorizontalAlignment="Right"/>
        </StackPanel>
    </Grid>
</Page>
